# 标注工具图标二次缩小设计

用户希望标注工具栏内的图标继续等比例缩小。为避免视觉缩小同时损害点击效率，本次只缩小图标内容，不改变 32×30 点的按钮点击热区。

- SF Symbol：12 pt → 10 pt。
- 文字工具 `T`：14 pt → 12 pt。
- 图标字重、按钮间距、颜色控件和工具栏位置保持不变。
- 验收：所有图标完整位于按钮中央，点击区域不变，工具栏布局测试与完整测试通过。
